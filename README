currently using master branch

The songs listing in the last 2 years has been a csv file grabbed from the triple j site

cap production deploy

ssh tserver1
su -l deploy
bundle exec rake db:seed RAILS_ENV=production
