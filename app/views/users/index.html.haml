%h1 Listing users
- if @song
  %h2#song_title Who picked
  %h2=link_to @song.name_and_artist, @song
%table
  %tr
    %th Name
    %th QTY Picked
    %th Score

  - @users.each do |user|
    %tr
      %td= link_to user.name, user
      %td.right= link_to user.selections.count, user_selections_path(user)
      %td.right= user.score

%br
- if can?(:add, User) && @song.nil?
  = link_to 'New User', new_user_path, id: "new_link"
